import React from "react";
import { initializeIcons } from "@uifabric/icons";
import { storiesOf } from "@storybook/react";
import { action } from "@storybook/addon-actions";

initializeIcons();

storiesOf("Sample", module).add("Initial", () => {});
